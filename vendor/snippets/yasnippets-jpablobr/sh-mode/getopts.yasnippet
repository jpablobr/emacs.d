# -*- mode: snippet -*-
# name: getopts
# --

if [ "\$1" = "--help" ]; then
	helptext
fi

while getopts ":hif:" opt; do
	case $opt in
		f )	echo "something todo with -f"
			;;
		i )	echo "something todo with -i"
			;;
		h )	helptext
			;;
		* )	usage
			exit 1
	esac
done;:
