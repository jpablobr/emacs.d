# -*- mode: snippet -*-
#key: c..1
#name : basic auth
# --
  before_filter :authenticate, :except => [:index, :show, :create]

private  
  def authenticate
    authenticate_or_request_with_http_basic do |name, password|  
      name == "admin" && password == "open"
    end  
  end

